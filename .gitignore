# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/
*.swp

# Env
.env
.env.local
.env.*.local

# Python
__pycache__/
*.pyc
.venv/
venv/
.mypy_cache/
.ruff_cache/
.pytest_cache/
.coverage

# Node
node_modules/
dist/
build/

# Expo
.expo/
.expo-shared/
web-build/

# Builds / native
ios/
android/

# Firebase config — committed so EAS Build can upload it.
# google-services.json is safe to commit: its API key is restricted by
# package name + SHA fingerprints and ships inside the APK anyway.
